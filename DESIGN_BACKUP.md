# LEXEM 디자인 백업 스냅샷

> 기준일: 2026-08-19 (KST)  
> 목적: 현재 UX/UI 기준을 재현·복원하기 위한 디자인 결정 기록

## 브랜드와 기본 톤

- 브랜드명은 `LEXEM`, 워드마크는 `assets/branding/lexem-wordmark-v2.png`를 사용한다.
- 시각 언어는 임상적 뉴트럴 컬러 위에 시그니처 바이올렛을 신호색으로 쓰는 방식이다.
- 기본 배경은 밝은 페이퍼 톤, 텍스트는 거의 검정에 가까운 잉크 톤으로 충분한 대비를 확보한다.
- 본문·라벨은 전역 텍스트 스케일 112%를 적용해 작은 화면에서도 읽기 쉽게 유지한다.

## 앱 시작 인트로

- 인트로에는 `LEXEM` 워드마크와 `YOUR SKIN, YOUR LANGUAGE`만 보여준다.
- 로고는 아래에서 위로 슬라이드하며 페이드 인한다.
- 슬로건은 로고보다 늦게 페이드 인한다.
- 시작 버튼은 두지 않는다. 진입 후 0.8초에 홈으로 전환되고, 전환은 420ms 페이드다.

## 홈: 성분 트렌드 히어로

- 3개 성분 슬라이드(레티날·세라마이드·비타민 C)를 자동 순환하고, 하단 인디케이터를 눌러 직접 이동할 수 있다.
- 상단의 `LIVE · INGREDIENT TREND`만 트렌드 정보를 제공한다. 카드 안의 중복 `TREND #` 및 증감률 표기는 사용하지 않는다.
- 하단 인디케이터는 바가 아닌 선택 상태가 커지는 원형 점으로 표시한다.
- 원형 점은 장식이 아닌 페이지네이션이다. 각 점은 넉넉한 터치 영역을 가지며 누르면 해당 성분 슬라이드로 이동한다.
- 모든 페이지 인디케이터는 공통 `PageIndicator`를 사용한다. 선택 상태는 30px 바이올렛 필, 비선택 상태는 11px 회색 원이며 클릭 시 해당 페이지로 이동한다.
- 히어로 CTA `지금 뜨는 성분 보기`는 오른쪽에 배치한다.
- CTA는 반투명 캡슐과 흰색 원형 `north east` 화살표로 구성한다.
- 성분 트렌드 히어로의 현재 확정 배경은 `assets/editorial/ingredient-trend-hero-editorial-v1.png`다. 딥 바이올렛 유리 분자 비주얼을 사용하고, 왼쪽 카피 영역은 진한 그라데이션으로 대비를 확보한다.
- 날씨 히어로에는 귀여운 날씨 일러스트와 둥실거리는 미세 모션을 사용한다.

## 성분 탐색 화면

- `지금 뜨는 성분`, `내 피부를 위한 성분 신호`, `이번 주 성분 리포트` 헤더는 같은 좌측 기준선에 정렬한다.
- 성분 레이더 필터는 가로 스크롤이 아니라 줄바꿈 칩 레이아웃을 사용해 모든 카테고리 라벨을 노출한다.
- 성분 검색은 단순 하단 필터가 아닌 백과사전형 검색이다. 입력 즉시 한국어·영문명·효능·요약을 기준으로 결과를 상단에 보여주고, 결과를 탭하면 상세 성분 해석을 연다.
- 주간 성분 리포트는 행 전체와 우측 북동쪽 화살표를 누를 수 있다.
- 리포트 선택 시 상세 내용을 담은 바텀시트를 연다.

## 홈: 브랜드 스토리와 피부 브리핑

- `피부를 읽는 다섯 가지 문법`의 각 카드는 탭 가능한 상세 해석 진입점이다.
- 복잡한 5개 문법 카드 대신, 현재 홈에는 `피부 확인 · 성분 해석 · 루틴 제안` 3개 핵심 케어 가이드를 간결한 타일로 보여준다.
- 우측 화살표는 장식이 아니라 상세 해석 바텀시트를 연다는 의미로 사용한다.
- 화살표는 바이올렛 원형 컨테이너 안의 북동쪽 아이콘으로 통일한다.
- `오늘의 피부 브리핑`은 넓은 화면에서 3열 카드, 좁은 화면에서는 캐러셀로 표시한다.
- 좁은 화면 캐러셀은 4초 자동 전환, 손가락 스와이프, 페이지 인디케이터를 제공한다.
- `어떤 신호가 가장 신경 쓰이나요?` 고민 필터는 줄바꿈 ChoiceChip으로 표시해 모든 항목이 잘리지 않게 한다.
- 고민 필터 영역의 배경은 그라데이션 없이 기본 페이퍼 단색을 유지한다.

## 날씨·피부 신호

- 홈의 피부 기상 리포트에는 사용자명, 피부 타입, 온도, UV, PM2.5를 반영한다.
- 자극 요인 카드는 원형 아이콘, 수치, 상태를 명확히 구분한다.
- 날씨·대기 데이터는 무료 데이터 조합을 사용하며, Worker 비밀키는 저장소에 포함하지 않는다.

## 인증

- Firebase Authentication 기반 이메일/Google 로그인을 제공한다.
- 로그인 모달에는 아이디 안내와 실제 비밀번호 재설정 진입을 둔다.

## Shop: 뷰티 캐비닛

- Shop 첫 화면은 큰 카피형 배너 대신, 메뉴·검색·브랜드 마크·저장·장바구니로 구성한 전용 미니 툴바를 사용한다.
- 제품 타입은 전체 폭의 아이콘·라벨·선택 밑줄 가로 카테고리 레일로 노출한다. 선택된 탭은 바이올렛 아이콘과 밑줄로 명확히 표시한다.
- 카테고리 탭(전체·세럼·크림·에센스·토너·앰플)은 실제 상품 목록 필터와 연결한다.
- 우측의 조절 아이콘은 장식이 아니라 세부 필터 진입점이다. 피부 고민·제품 타입·성분·브랜드 필터를 바텀시트에서 제공한다.
- 모바일에서는 제품 이미지 중심의 2열 그리드를 기본으로 해, 상품을 한눈에 비교할 수 있게 한다.

## 하단 네비게이션

- 하단 네비게이션은 화면 가장자리에서 12px 띄운 플로팅 글래스 독으로 구성한다.
- 반투명 화이트 레이어와 18px 배경 블러를 사용해, 뒤의 콘텐츠가 은은하게 비치는 글래스모피즘 효과를 만든다.
- 선택 탭은 연한 라벤더 인디케이터와 바이올렛 라벨로 표시하며, 나머지 탭은 뉴트럴 그레이로 유지한다.

## 커머스 흐름 (데모)

- 상품 상세에는 `장바구니`와 `바로 구매`를 함께 둔다.
- 장바구니에서는 주문 금액을 확인하고 결제를 진행한다. 현재는 외부 결제수단을 호출하지 않는 앱 내 구매 데모다.
- 결제 후 주문 상태 화면에서 `결제 완료 → 상품 준비 → 배송 중 → 배송 완료`를 조회한다.
- 배송 단계 데모 진행 버튼으로 상태를 순차적으로 확인할 수 있으며, 주문 내역은 My Skin에서도 다시 열 수 있다.

## AI 약사 챗봇 진입

- 챗봇은 보라색 확장 버튼 대신, 글래스 오브 형태의 플로팅 컨트롤을 사용한다.
- 반투명 `AI Pharmacist` 라벨과 원형 채팅 버튼을 분리하고, 버튼 우상단의 바이올렛 스파클 배지로 AI 케어 신호를 보여준다.
- 버튼·라벨 모두 실제 블러와 반투명 레이어로 구현하며, 탭하면 기존 AI 약사 챗봇을 연다.

## 복원 방법

1. 이 문서의 디자인 원칙과 대상 파일을 기준으로 UI를 확인한다.
2. Git 태그 또는 커밋을 기준으로 복원할 때는 `git log --oneline`에서 스냅샷 커밋을 찾는다.
3. 특정 파일만 되돌릴 경우 `git restore --source <commit> -- <path>`를 사용한다.
4. Worker의 `.wrangler/` 로컬 캐시와 모든 비밀키는 백업 대상에서 제외한다.

## 관련 핵심 파일

- `lib/main.dart`
- `lib/theme.dart`
- `lib/screens/intro_screen.dart`
- `lib/screens/home_sections.dart`
- `lib/screens/discover_screen.dart`
- `lib/screens/discover_sections.dart`
- `lib/screens/skin_weather_screen.dart`
- `lib/screens/shop_screen.dart`
- `assets/branding/lexem-wordmark-v2.png`
- `assets/editorial/skin-weather-hero-cute-v1.png`
- `assets/editorial/ingredient-trend-hero-editorial-v1.png`
